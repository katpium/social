export default function NavBar() {
    return (
        <nav className="nav-bar">
        <a href="#home">Home</a>
        <a href="#browse">Browse</a>
        <a href="#search">Search</a>
      </nav>
    );
}